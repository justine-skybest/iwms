/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ProductSummaryDtoPaginatedResponse } from '../../models/product-summary-dto-paginated-response';

export interface ProductV2Get$Params {
  search?: string;
  page?: number;
  pageSize?: number;
}

export function productV2Get(http: HttpClient, rootUrl: string, params?: ProductV2Get$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductSummaryDtoPaginatedResponse>> {
  const rb = new RequestBuilder(rootUrl, productV2Get.PATH, 'get');
  if (params) {
    rb.query('search', params.search, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductSummaryDtoPaginatedResponse>;
    })
  );
}

productV2Get.PATH = '/product/v2';
