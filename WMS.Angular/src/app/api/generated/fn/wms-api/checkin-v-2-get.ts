/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CheckInSummaryDtoPaginatedResponse } from '../../models/check-in-summary-dto-paginated-response';

export interface CheckinV2Get$Params {
  search?: string;
  warehouseId?: number;
  page?: number;
  pageSize?: number;
}

export function checkinV2Get(http: HttpClient, rootUrl: string, params?: CheckinV2Get$Params, context?: HttpContext): Observable<StrictHttpResponse<CheckInSummaryDtoPaginatedResponse>> {
  const rb = new RequestBuilder(rootUrl, checkinV2Get.PATH, 'get');
  if (params) {
    rb.query('search', params.search, {});
    rb.query('warehouseId', params.warehouseId, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CheckInSummaryDtoPaginatedResponse>;
    })
  );
}

checkinV2Get.PATH = '/checkin/v2';
