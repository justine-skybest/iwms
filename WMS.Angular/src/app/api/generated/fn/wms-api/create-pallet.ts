/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CreatePalletDto } from '../../models/create-pallet-dto';
import type { PalletSummaryDto } from '../../models/pallet-summary-dto';

export interface CreatePallet$Params {
      body: CreatePalletDto
}

export function createPallet(http: HttpClient, rootUrl: string, params: CreatePallet$Params, context?: HttpContext): Observable<StrictHttpResponse<PalletSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, createPallet.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PalletSummaryDto>;
    })
  );
}

createPallet.PATH = '/pallet';
