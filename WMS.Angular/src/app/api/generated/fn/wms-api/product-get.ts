/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ProductSummaryDto } from '../../models/product-summary-dto';

export interface ProductGet$Params {
}

export function productGet(http: HttpClient, rootUrl: string, params?: ProductGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProductSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, productGet.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProductSummaryDto>>;
    })
  );
}

productGet.PATH = '/product';
