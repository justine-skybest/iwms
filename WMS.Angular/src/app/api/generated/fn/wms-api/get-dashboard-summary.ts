/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { DashboardSummaryDto } from '../../models/dashboard-summary-dto';

export interface GetDashboardSummary$Params {
  warehouseId?: number;
}

export function getDashboardSummary(http: HttpClient, rootUrl: string, params?: GetDashboardSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<DashboardSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, getDashboardSummary.PATH, 'get');
  if (params) {
    rb.query('warehouseId', params.warehouseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DashboardSummaryDto>;
    })
  );
}

getDashboardSummary.PATH = '/dashboard/summary';
