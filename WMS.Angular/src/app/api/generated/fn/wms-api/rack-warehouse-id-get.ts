/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { RackSummaryDto } from '../../models/rack-summary-dto';

export interface RackWarehouseIdGet$Params {
  id: number;
}

export function rackWarehouseIdGet(http: HttpClient, rootUrl: string, params: RackWarehouseIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RackSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, rackWarehouseIdGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RackSummaryDto>>;
    })
  );
}

rackWarehouseIdGet.PATH = '/rack/warehouse/{id}';
