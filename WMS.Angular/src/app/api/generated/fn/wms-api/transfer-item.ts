/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { TransferItemDto } from '../../models/transfer-item-dto';
import type { TransferResultDto } from '../../models/transfer-result-dto';

export interface TransferItem$Params {
      body: TransferItemDto
}

export function transferItem(http: HttpClient, rootUrl: string, params: TransferItem$Params, context?: HttpContext): Observable<StrictHttpResponse<TransferResultDto>> {
  const rb = new RequestBuilder(rootUrl, transferItem.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TransferResultDto>;
    })
  );
}

transferItem.PATH = '/transfers/item';
