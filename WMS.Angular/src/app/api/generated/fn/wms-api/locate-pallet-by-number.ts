/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PalletLocationDto } from '../../models/pallet-location-dto';

export interface LocatePalletByNumber$Params {
  palletNumber: number;
  warehouseId: number;
}

export function locatePalletByNumber(http: HttpClient, rootUrl: string, params: LocatePalletByNumber$Params, context?: HttpContext): Observable<StrictHttpResponse<PalletLocationDto>> {
  const rb = new RequestBuilder(rootUrl, locatePalletByNumber.PATH, 'get');
  if (params) {
    rb.path('palletNumber', params.palletNumber, {});
    rb.path('warehouseId', params.warehouseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PalletLocationDto>;
    })
  );
}

locatePalletByNumber.PATH = '/transfers/pallet/locate/number/{palletNumber}/{warehouseId}';
