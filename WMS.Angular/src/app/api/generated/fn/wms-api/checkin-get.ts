/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { CheckInSummaryDto } from '../../models/check-in-summary-dto';

export interface CheckinGet$Params {
}

export function checkinGet(http: HttpClient, rootUrl: string, params?: CheckinGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CheckInSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, checkinGet.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CheckInSummaryDto>>;
    })
  );
}

checkinGet.PATH = '/checkin';
