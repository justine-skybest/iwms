/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { ItemLocationSummaryDto } from '../../models/item-location-summary-dto';

export interface GetItemsInBin$Params {
  binId: number;
}

export function getItemsInBin(http: HttpClient, rootUrl: string, params: GetItemsInBin$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ItemLocationSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, getItemsInBin.PATH, 'get');
  if (params) {
    rb.path('binId', params.binId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ItemLocationSummaryDto>>;
    })
  );
}

getItemsInBin.PATH = '/transfers/item/bin/{binId}';
