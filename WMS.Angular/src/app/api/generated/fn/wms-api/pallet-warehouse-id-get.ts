/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { PalletSummaryDto } from '../../models/pallet-summary-dto';

export interface PalletWarehouseIdGet$Params {
  id: number;
}

export function palletWarehouseIdGet(http: HttpClient, rootUrl: string, params: PalletWarehouseIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PalletSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, palletWarehouseIdGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PalletSummaryDto>>;
    })
  );
}

palletWarehouseIdGet.PATH = '/pallet/Warehouse/{id}';
