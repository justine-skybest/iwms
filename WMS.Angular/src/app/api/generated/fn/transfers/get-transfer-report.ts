/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { TransferLogDto } from '../../models/transfer-log-dto';

export interface GetTransferReport$Params {
  from: string;
  to: string;
  warehouseId?: number;
}

export function getTransferReport(http: HttpClient, rootUrl: string, params: GetTransferReport$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TransferLogDto>>> {
  const rb = new RequestBuilder(rootUrl, getTransferReport.PATH, 'get');
  if (params) {
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('warehouseId', params.warehouseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TransferLogDto>>;
    })
  );
}

getTransferReport.PATH = '/transfers/report';
