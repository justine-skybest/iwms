/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { IncomingResponseDtoPaginatedResponse } from '../../models/incoming-response-dto-paginated-response';

export interface GetUnreceivedIncomings$Params {
  search?: string;
  warehouseId?: number;
  page?: number;
  pageSize?: number;
}

export function getUnreceivedIncomings(http: HttpClient, rootUrl: string, params?: GetUnreceivedIncomings$Params, context?: HttpContext): Observable<StrictHttpResponse<IncomingResponseDtoPaginatedResponse>> {
  const rb = new RequestBuilder(rootUrl, getUnreceivedIncomings.PATH, 'get');
  if (params) {
    rb.query('search', params.search, {});
    rb.query('warehouseId', params.warehouseId, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncomingResponseDtoPaginatedResponse>;
    })
  );
}

getUnreceivedIncomings.PATH = '/incoming/unreceived';
