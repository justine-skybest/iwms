/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { IncomingRequestDto } from '../../models/incoming-request-dto';
import type { IncomingResponseDto } from '../../models/incoming-response-dto';

export interface CreateIncoming$Params {
      body: IncomingRequestDto
}

export function createIncoming(http: HttpClient, rootUrl: string, params: CreateIncoming$Params, context?: HttpContext): Observable<StrictHttpResponse<IncomingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, createIncoming.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncomingResponseDto>;
    })
  );
}

createIncoming.PATH = '/incoming';
