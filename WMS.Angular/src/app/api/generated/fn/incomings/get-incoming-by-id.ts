/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { IncomingResponseDto } from '../../models/incoming-response-dto';

export interface GetIncomingById$Params {
  id: number;
}

export function getIncomingById(http: HttpClient, rootUrl: string, params: GetIncomingById$Params, context?: HttpContext): Observable<StrictHttpResponse<IncomingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, getIncomingById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncomingResponseDto>;
    })
  );
}

getIncomingById.PATH = '/incoming/{id}';
