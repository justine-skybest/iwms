/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import type { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import type { InventoryInspectionResultDto } from '../../models/inventory-inspection-result-dto';

export interface InspectInventoryCode$Params {
  code: string;
  warehouseId: number;
}

export function inspectInventoryCode(http: HttpClient, rootUrl: string, params: InspectInventoryCode$Params, context?: HttpContext): Observable<StrictHttpResponse<InventoryInspectionResultDto>> {
  const rb = new RequestBuilder(rootUrl, inspectInventoryCode.PATH, 'get');
  if (params) {
    rb.path('code', params.code, {});
    rb.path('warehouseId', params.warehouseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InventoryInspectionResultDto>;
    })
  );
}

inspectInventoryCode.PATH = '/inventory/inspect/{code}/{warehouseId}';
